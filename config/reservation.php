<?php

/*
|--------------------------------------------------------------------------
| 予約まわりの運用設定
|--------------------------------------------------------------------------
|
| 運用で変わりうる数値をここ 1 か所に集める（設計書 6. / DEC-016）。
| 画面の表示（キャンセル期限の案内・空き状況の色分け）とサーバ側の判定は、
| どちらもこの値を見る。
|
*/

return [

    /*
    |--------------------------------------------------------------------------
    | キャンセル期限
    |--------------------------------------------------------------------------
    |
    | レッスン開始の何時間前までキャンセルできるか。会員向けの案内文にも使う。
    | キャンセルの実処理は STEP5 で、この値を使って判定する。
    |
    */

    'cancel_deadline_hours' => (int) env('RESERVATION_CANCEL_DEADLINE_HOURS', 2),

    /*
    |--------------------------------------------------------------------------
    | 「残りわずか」と見なす埋まり具合
    |--------------------------------------------------------------------------
    |
    | 予約数 ÷ 定員 がこの割合以上になったら「残りわずか」（アンバー）で表示する。
    | 空き枠カレンダーのゲージの色分け（DEC-014）に使う。
    |
    */

    'few_seats_ratio' => 0.75,

];
