<?php

/*
|--------------------------------------------------------------------------
| テーマ設定（差し替え口）
|--------------------------------------------------------------------------
|
| サービス名・ロゴ・配色をここ 1 か所で切り替えられる。
| 顧客ごとに見た目を変える場合は .env の THEME_* を書き換えるだけでよく、
| アセットの再ビルドは不要（配色は CSS カスタムプロパティとして実行時に注入される）。
|
| 詳しい手順は README の「テーマを差し替える」を参照。
|
*/

return [

    /*
    |--------------------------------------------------------------------------
    | サービス名
    |--------------------------------------------------------------------------
    |
    | ヘッダー・ブラウザのタブ・ログイン画面に表示される名称。
    | 未指定なら APP_NAME を使う。
    |
    */

    'name' => env('THEME_NAME', env('APP_NAME', 'Business Template')),

    /*
    |--------------------------------------------------------------------------
    | サブタイトル
    |--------------------------------------------------------------------------
    |
    | ログイン画面などに小さく添える説明。空文字なら非表示。
    |
    */

    'tagline' => env('THEME_TAGLINE', '業務システム共通基盤'),

    /*
    |--------------------------------------------------------------------------
    | ロゴ
    |--------------------------------------------------------------------------
    |
    | public/ 配下の画像パス（例: 'images/logo.svg'）。
    | null の場合はサービス名の頭文字を使った既定のマークを表示する。
    |
    */

    'logo' => env('THEME_LOGO'),

    /*
    |--------------------------------------------------------------------------
    | 配色
    |--------------------------------------------------------------------------
    |
    | CSS の色として有効な値（#rrggbb / rgb() / oklch() など）。
    | ここを変えると、ボタン・リンク・バッジ・グラフの色がまとめて切り替わる。
    | 淡い背景色や hover 時の色は color-mix() で自動的に派生する。
    |
    */

    'colors' => [
        // 主要な操作（保存ボタン、選択中のメニュー、リンク）
        'primary' => env('THEME_PRIMARY', '#4f46e5'),

        // 補助的な強調（グラフの 2 色目など）
        'accent' => env('THEME_ACCENT', '#0891b2'),
    ],

    /*
    |--------------------------------------------------------------------------
    | グラフの配色
    |--------------------------------------------------------------------------
    |
    | 先頭 2 色は primary / accent が自動で入る（null の位置に差し込まれる）。
    | 3 色目以降は系統ごとに区別しやすい固定色。
    |
    */

    // 白背景でも輪郭が分かるよう、WCAG の非テキストコントラスト(3:1)を満たす濃さにしてある
    'chart_palette' => [
        null,
        null,
        '#d97706',
        '#059669',
        '#dc2626',
        '#7c3aed',
        '#475569',
    ],

];
